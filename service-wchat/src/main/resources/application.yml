server:
  port: 8073

spring:
  application:
    name: @artifactId@
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://xfyx:xfyx@localhost:8081/eureka/

